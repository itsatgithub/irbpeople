<?xml version="1.0" encoding="ISO-8859-1"?>
<report view="auditmessage" site-number="3">
    <site name="en">
        <!-- Englobamos en '<fields>' la manipulacion de los campos en las 3 zonas del report: SELECT, WHERE y ORDERBY. -->
        <fields>
            <!-- ****************************************** -->
            <!-- - Campos visibles en la seccion de SELECT 	-->
            <!-- ****************************************** -->
            <select-fields />
            <!-- ****************************************** -->
            <!-- - Campos visibles en la seccion de WHERE 	-->
            <!-- ****************************************** -->
            <where-fields>
                <field name="timestamp" visible="true" default="true" type-of-field="date">
                    <caption language="en">timestamp</caption>
                </field>
                <field name="message" visible="true" default="true" type-of-field="text">
                    <caption language="en">message</caption>
                </field>
                <field name="type.description" visible="true" default="true" type-of-field="text">
                    <caption language="en">type</caption>
                </field>
                <field name="user.username" visible="true" default="true" type-of-field="text">
                    <caption language="en">Usercode</caption>
                </field>
            </where-fields>
            <!-- ******************************************** -->
            <!-- - Campos visibles en la seccion de ORDERBY   -->
            <!-- ******************************************** -->
            <orderby-fields />
        </fields>
    </site>
    <site name="ct">
        <!-- Englobamos en '<fields>' la manipulacion de los campos en las 3 zonas del report: SELECT, WHERE y ORDERBY. -->
        <fields>
            <!-- ****************************************** -->
            <!-- - Campos visibles en la seccion de SELECT 	-->
            <!-- ****************************************** -->
            <select-fields />
            <!-- ****************************************** -->
            <!-- - Campos visibles en la seccion de WHERE 	-->
            <!-- ****************************************** -->
            <where-fields>
                <field name="timestamp" visible="true" default="true" type-of-field="date">
                    <caption language="ct">timestamp</caption>
                </field>
                <field name="message" visible="true" default="true" type-of-field="text">
                    <caption language="ct">missatge</caption>
                </field>
                <field name="type.description" visible="true" default="true" type-of-field="text">
                    <caption language="ct">tipus</caption>
                </field>
                <field name="user.username" visible="true" default="true" type-of-field="text">
                    <caption language="ct">Usercode</caption>
                </field>
            </where-fields>
            <!-- ******************************************** -->
            <!-- - Campos visibles en la seccion de ORDERBY   -->
            <!-- ******************************************** -->
            <orderby-fields />
        </fields>
    </site>
    <site name="es">
        <!-- Englobamos en '<fields>' la manipulacion de los campos en las 3 zonas del report: SELECT, WHERE y ORDERBY. -->
        <fields>
            <!-- ****************************************** -->
            <!-- - Campos visibles en la seccion de SELECT 	-->
            <!-- ****************************************** -->
            <select-fields />
            <!-- ****************************************** -->
            <!-- - Campos visibles en la seccion de WHERE 	-->
            <!-- ****************************************** -->
            <where-fields>
                <field name="timestamp" visible="true" default="true" type-of-field="date">
                    <caption language="es">timestamp</caption>
                </field>
                <field name="message" visible="true" default="true" type-of-field="text">
                    <caption language="es">mensaje</caption>
                </field>
                <field name="type.description" visible="true" default="true" type-of-field="text">
                    <caption language="es">tipo</caption>
                </field>
                <field name="user.username" visible="true" default="true" type-of-field="text">
                    <caption language="es">Usercode</caption>
                </field>
            </where-fields>
            <!-- ******************************************** -->
            <!-- - Campos visibles en la seccion de ORDERBY   -->
            <!-- ******************************************** -->
            <orderby-fields />
        </fields>
    </site>
</report>

