<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- Nota: El nombre del objeto (nombre_del_objeto) es el nombre en minusculas de la tabla en base de datos. 	-->
<!-- Nota: En general, si un atributo de tipo 'boolean' no se encuentra se considera su valor como 'false'. 	-->
<item-list obj="customlist" site-number="3">
    <desc>Descripcion para la lista del objeto 'customlist'.</desc>
    <!-- Categorizaciones para poder agrupar los ficheros de configuracion. -->
    <!-- Para agregar una categoria poner un atributo 'attName=attValue'.   -->
    <category />
    <site name="en">
        <label language="es">Etiqueta de visualizacion para el objeto 'customlist'.</label>
        <comment language="es">Descripcion de visualizacion para el objeto 'customlist'.</comment>
        <title language="es">Titulo de visualizacion para el objeto 'customlist'.</title>
        <!-- Campos del objeto para visualizacion -->
        <fields>
            <!-- - Campos visibles -->
            <!-- El orden es el de aparicion de los tags. Para editar el orden se recomienda utilizar el XMLSpy, -->
            <!-- mediante el cual, es sencillo mover un tag arriba y abajo. -->
            <!-- Todos los atributos son modificables, pero algunos de ellos (label por ejemplo), se deberian modificar en otro sitio -->
            <!-- Los atributos modificables son: 'default' y 'visible'.  -->
            <visible-fields>
                <field name="customListcode" visible="true" default="true" type-of-field="text" size="10">
                    <caption language="en">Code</caption>
                </field>
                <field name="name" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">Name</caption>
                </field>
                <field name="usercode" visible="true" default="true" type-of-field="text" size="10">
                    <caption language="en">User</caption>
                </field>
                <field name="update_date" visible="true" default="true" type-of-field="text" size="15">
                    <caption language="en">Date of modification</caption>
                </field>
                <field name="comments" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">Comments</caption>
                </field>
                <field name="periodic" visible="true" default="true" type-of-field="boolean" size="7">
                    <caption language="en">Periodic</caption>
                </field>
            </visible-fields>
            <!-- - Campos NO visibles - NO MODIFICAR, ES SOLO A NIVEL INFORMATIVO -->
            <!-- 1/2 - Campos 'hidden'. -->
            <hidden-fields />
            <!-- 2/2 - Campos 'discarded'. -->
            <discarded-fields />
        </fields>
    </site>
    <site name="ct">
        <label language="es">Etiqueta de visualizacion para el objeto 'customlist'.</label>
        <comment language="es">Descripcion de visualizacion para el objeto 'customlist'.</comment>
        <title language="es">Titulo de visualizacion para el objeto 'customlist'.</title>
        <!-- Campos del objeto para visualizacion -->
        <fields>
            <!-- - Campos visibles -->
            <!-- El orden es el de aparicion de los tags. Para editar el orden se recomienda utilizar el XMLSpy, -->
            <!-- mediante el cual, es sencillo mover un tag arriba y abajo. -->
            <!-- Todos los atributos son modificables, pero algunos de ellos (label por ejemplo), se deberian modificar en otro sitio -->
            <!-- Los atributos modificables son: 'default' y 'visible'.  -->
            <visible-fields>
                <field name="customListcode" visible="true" default="true" type-of-field="text" size="10">
                    <caption language="ct">Codi</caption>
                </field>
                <field name="name" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Nom</caption>
                </field>
                <field name="usercode" visible="true" default="true" type-of-field="text" size="10">
                    <caption language="ct">Usuari</caption>
                </field>
                <field name="update_date" visible="true" default="true" type-of-field="text" size="15">
                    <caption language="ct">Data de modificació</caption>
                </field>
                <field name="comments" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Comentaris</caption>
                </field>
                <field name="periodic" visible="true" default="true" type-of-field="boolean" size="7">
                    <caption language="ct">Periodic</caption>
                </field>
            </visible-fields>
            <!-- - Campos NO visibles - NO MODIFICAR, ES SOLO A NIVEL INFORMATIVO -->
            <!-- 1/2 - Campos 'hidden'. -->
            <hidden-fields />
            <!-- 2/2 - Campos 'discarded'. -->
            <discarded-fields />
        </fields>
    </site>
    <site name="es">
        <label language="es">Etiqueta de visualizacion para el objeto 'customlist'.</label>
        <comment language="es">Descripcion de visualizacion para el objeto 'customlist'.</comment>
        <title language="es">Titulo de visualizacion para el objeto 'customlist'.</title>
        <!-- Campos del objeto para visualizacion -->
        <fields>
            <!-- - Campos visibles -->
            <!-- El orden es el de aparicion de los tags. Para editar el orden se recomienda utilizar el XMLSpy, -->
            <!-- mediante el cual, es sencillo mover un tag arriba y abajo. -->
            <!-- Todos los atributos son modificables, pero algunos de ellos (label por ejemplo), se deberian modificar en otro sitio -->
            <!-- Los atributos modificables son: 'default' y 'visible'.  -->
            <visible-fields>
               <field name="customListcode" visible="true" default="true" type-of-field="text" size="10">
                    <caption language="es">Código</caption>
                </field>
                <field name="name" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Nombre</caption>
                </field>
                <field name="usercode" visible="true" default="true" type-of-field="text" size="10">
                    <caption language="es">Usuario</caption>
                </field>
                <field name="update_date" visible="true" default="true" type-of-field="text" size="15">
                    <caption language="es">Fecha de modificación</caption>
                </field>
                <field name="comments" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Comentarios</caption>
                </field>
                <field name="periodic" visible="true" default="true" type-of-field="boolean" size="7">
                    <caption language="en">Periodico</caption>
                </field>
            </visible-fields>
            <!-- - Campos NO visibles - NO MODIFICAR, ES SOLO A NIVEL INFORMATIVO -->
            <!-- 1/2 - Campos 'hidden'. -->
            <hidden-fields />
            <!-- 2/2 - Campos 'discarded'. -->
            <discarded-fields />
        </fields>
    </site>
</item-list>


