<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- Nota: El nombre del objeto (nombre_del_objeto) es el nombre en minusculas de la tabla en base de datos. 	-->
<!-- Nota: En general, si un atributo de tipo 'boolean' no se encuentra se considera su valor como 'false'. 	-->
<item-list obj="benefits" site-number="3">
    <desc>Descripcion para la lista del objeto 'benefits'.</desc>
    <!-- Categorizaciones para poder agrupar los ficheros de configuracion. -->
    <!-- Para agregar una categoria poner un atributo 'attName=attValue'.   -->
    <category />
    <site name="en">
        <label language="es">Etiqueta de visualizacion para el objeto 'benefits'.</label>
        <comment language="es">Descripcion de visualizacion para el objeto 'benefits'.</comment>
        <title language="es">Titulo de visualizacion para el objeto 'benefits'.</title>
        <!-- Campos del objeto para visualizacion -->
        <fields>
            <!-- - Campos visibles -->
            <!-- El orden es el de aparicion de los tags. Para editar el orden se recomienda utilizar el XMLSpy, -->
            <!-- mediante el cual, es sencillo mover un tag arriba y abajo. -->
            <!-- Todos los atributos son modificables, pero algunos de ellos (label por ejemplo), se deberian modificar en otro sitio -->
            <!-- Los atributos modificables son: 'default' y 'visible'.  -->
            <visible-fields>
                <field name="start_date" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">Start date</caption>
                </field>
                <field name="end_date" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">End date</caption>
                </field>
                <field name="description" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">Description</caption>
                </field>
                <field name="type_of_benefit" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">Type of benefit</caption>
                </field>
            </visible-fields>
            <!-- - Campos NO visibles - NO MODIFICAR, ES SOLO A NIVEL INFORMATIVO -->
            <!-- 1/2 - Campos 'hidden'. -->
            <hidden-fields />
            <!-- 2/2 - Campos 'discarded'. -->
            <discarded-fields />
        </fields>
    </site>
    <site name="ct">
        <label language="es">Etiqueta de visualizacion para el objeto 'benefits'.</label>
        <comment language="es">Descripcion de visualizacion para el objeto 'benefits'.</comment>
        <title language="es">Titulo de visualizacion para el objeto 'benefits'.</title>
        <!-- Campos del objeto para visualizacion -->
        <fields>
            <!-- - Campos visibles -->
            <!-- El orden es el de aparicion de los tags. Para editar el orden se recomienda utilizar el XMLSpy, -->
            <!-- mediante el cual, es sencillo mover un tag arriba y abajo. -->
            <!-- Todos los atributos son modificables, pero algunos de ellos (label por ejemplo), se deberian modificar en otro sitio -->
            <!-- Los atributos modificables son: 'default' y 'visible'.  -->
            <visible-fields>
                <field name="start_date" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Data d'inici</caption>
                </field>
                <field name="end_date" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Data de fi</caption>
                </field>
                <field name="description" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Descripció</caption>
                </field>
                <field name="type_of_benefit" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Tipus de benefici</caption>
                </field>
            </visible-fields>
            <!-- - Campos NO visibles - NO MODIFICAR, ES SOLO A NIVEL INFORMATIVO -->
            <!-- 1/2 - Campos 'hidden'. -->
            <hidden-fields />
            <!-- 2/2 - Campos 'discarded'. -->
            <discarded-fields />
        </fields>
    </site>
    <site name="es">
        <label language="es">Etiqueta de visualizacion para el objeto 'benefits'.</label>
        <comment language="es">Descripcion de visualizacion para el objeto 'benefits'.</comment>
        <title language="es">Titulo de visualizacion para el objeto 'benefits'.</title>
        <!-- Campos del objeto para visualizacion -->
        <fields>
            <!-- - Campos visibles -->
            <!-- El orden es el de aparicion de los tags. Para editar el orden se recomienda utilizar el XMLSpy, -->
            <!-- mediante el cual, es sencillo mover un tag arriba y abajo. -->
            <!-- Todos los atributos son modificables, pero algunos de ellos (label por ejemplo), se deberian modificar en otro sitio -->
            <!-- Los atributos modificables son: 'default' y 'visible'.  -->
            <visible-fields>
                <field name="start_date" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Fecha de inicio</caption>
                </field>
                <field name="end_date" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Fecha de fin</caption>
                </field>
                <field name="description" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Descripción</caption>
                </field>
                <field name="type_of_benefit" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Tipo de beneficio</caption>
                </field>
            </visible-fields>
            <!-- - Campos NO visibles - NO MODIFICAR, ES SOLO A NIVEL INFORMATIVO -->
            <!-- 1/2 - Campos 'hidden'. -->
            <hidden-fields />
            <!-- 2/2 - Campos 'discarded'. -->
            <discarded-fields />
        </fields>
    </site>
</item-list>

