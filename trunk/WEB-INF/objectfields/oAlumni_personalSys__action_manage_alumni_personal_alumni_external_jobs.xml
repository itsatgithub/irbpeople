<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- Nota: El nombre del objeto (nombre_del_objeto) es el nombre en minusculas de la tabla en base de datos. 	-->
<!-- Nota: En general, si un atributo de tipo 'boolean' no se encuentra se considera su valor como 'false'. 	-->
<item-list obj="alumni_external_jobs" site-number="3">
    <desc>Descripcion para la lista del objeto 'alumni_external_jobs'.</desc>
    <!-- Categorizaciones para poder agrupar los ficheros de configuracion. -->
    <!-- Para agregar una categoria poner un atributo 'attName=attValue'.   -->
    <category />
    <site name="en">
        <label language="es">Etiqueta de visualizacion para el objeto 'alumni_external_jobs'.</label>
        <comment language="es">Descripcion de visualizacion para el objeto 'alumni_external_jobs'.</comment>
        <title language="es">Titulo de visualizacion para el objeto 'alumni_external_jobs'.</title>
        <!-- Campos del objeto para visualizacion -->
        <fields>
            <!-- - Campos visibles -->
            <!-- El orden es el de aparicion de los tags. Para editar el orden se recomienda utilizar el XMLSpy, -->
            <!-- mediante el cual, es sencillo mover un tag arriba y abajo. -->
            <!-- Todos los atributos son modificables, pero algunos de ellos (label por ejemplo), se deberian modificar en otro sitio -->
            <!-- Los atributos modificables son: 'default' y 'visible'.  -->
            <visible-fields>
                <field name="current" visible="true" default="true" type-of-field="boolean" size="7">
                    <caption language="en">Current</caption>
                </field>
                <field name="external_job_positions" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">External job positions</caption>
                </field>
                <field name="external_job_sectors" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">External job sectors</caption>
                </field>
                <field name="institution" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">Institution</caption>
                </field>
                <field name="city" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">City</caption>
                </field>
                <field name="country" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">Country</caption>
                </field>
                <field name="start_date" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">Start date</caption>
                </field>
                <field name="end_date" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">End date</caption>
                </field>
            </visible-fields>
            <!-- - Campos NO visibles - NO MODIFICAR, ES SOLO A NIVEL INFORMATIVO -->
            <!-- 1/2 - Campos 'hidden'. -->
            <hidden-fields />
            <!-- 2/2 - Campos 'discarded'. -->
            <discarded-fields />
        </fields>
    </site>
    <site name="ct">
        <label language="ct">Etiqueta de visualizacion para el objeto 'alumni_external_jobs'.</label>
        <comment language="ct">Descripcion de visualizacion para el objeto 'alumni_external_jobs'.</comment>
        <title language="ct">Titulo de visualizacion para el objeto 'alumni_external_jobs'.</title>
        <!-- Campos del objeto para visualizacion -->
        <fields>
            <!-- - Campos visibles -->
            <!-- El orden es el de aparicion de los tags. Para editar el orden se recomienda utilizar el XMLSpy, -->
            <!-- mediante el cual, es sencillo mover un tag arriba y abajo. -->
            <!-- Todos los atributos son modificables, pero algunos de ellos (label por ejemplo), se deberian modificar en otro sitio -->
            <!-- Los atributos modificables son: 'default' y 'visible'.  -->
            <visible-fields>
                <field name="current" visible="true" default="true" type-of-field="boolean" size="7">
                    <caption language="ct">Actual</caption>
                </field>
                <field name="external_job_positions" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Càrrec extern</caption>
                </field>
                <field name="external_job_sectors" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Sector extern</caption>
                </field>
                <field name="institution" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Institució</caption>
                </field>
                <field name="city" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Ciutat</caption>
                </field>
                <field name="country" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">País</caption>
                </field>
                <field name="start_date" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Data inici</caption>
                </field>
                <field name="end_date" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Data fi</caption>
                </field>
            </visible-fields>
            <!-- - Campos NO visibles - NO MODIFICAR, ES SOLO A NIVEL INFORMATIVO -->
            <!-- 1/2 - Campos 'hidden'. -->
            <hidden-fields />
            <!-- 2/2 - Campos 'discarded'. -->
            <discarded-fields />
        </fields>
    </site>
    <site name="es">
        <label language="es">Etiqueta de visualizacion para el objeto 'alumni_external_jobs'.</label>
        <comment language="es">Descripcion de visualizacion para el objeto 'alumni_external_jobs'.</comment>
        <title language="es">Titulo de visualizacion para el objeto 'alumni_external_jobs'.</title>
        <!-- Campos del objeto para visualizacion -->
        <fields>
            <!-- - Campos visibles -->
            <!-- El orden es el de aparicion de los tags. Para editar el orden se recomienda utilizar el XMLSpy, -->
            <!-- mediante el cual, es sencillo mover un tag arriba y abajo. -->
            <!-- Todos los atributos son modificables, pero algunos de ellos (label por ejemplo), se deberian modificar en otro sitio -->
            <!-- Los atributos modificables son: 'default' y 'visible'.  -->
            <visible-fields>
              	<field name="current" visible="true" default="true" type-of-field="boolean" size="7">
                    <caption language="es">Actual</caption>
                </field>
                <field name="external_job_positions" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Cargo externo</caption>
                </field>
                <field name="external_job_sectors" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Sector externo</caption>
                </field>
                <field name="institution" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Institución</caption>
                </field>
                <field name="city" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Ciudad</caption>
                </field>
                <field name="country" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">País</caption>
                </field>
                <field name="start_date" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Fecha de inicio</caption>
                </field>
                <field name="end_date" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Fecha de fin</caption>
                </field>
            </visible-fields>
            <!-- - Campos NO visibles - NO MODIFICAR, ES SOLO A NIVEL INFORMATIVO -->
            <!-- 1/2 - Campos 'hidden'. -->
            <hidden-fields />
            <!-- 2/2 - Campos 'discarded'. -->
            <discarded-fields />
        </fields>
    </site>
</item-list>

