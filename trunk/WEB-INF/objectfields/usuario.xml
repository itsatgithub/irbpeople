<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- Nota: El nombre del objeto (nombre_del_objeto) es el nombre en minusculas de la tabla en base de datos. 	-->
<!-- Nota: En general, si un atributo de tipo 'boolean' no se encuentra se considera su valor como 'false'. 	-->
<item-list obj="usuario" site-number="3">
    <desc>Descripcion para la lista del objeto 'usuario'.</desc>
    <!-- Categorizaciones para poder agrupar los ficheros de configuracion. -->
    <!-- Para agregar una categoria poner un atributo 'attName=attValue'.   -->
    <category />
    <site name="en">
        <label language="es">Etiqueta de visualizacion para el objeto 'usuario'.</label>
        <comment language="es">Descripcion de visualizacion para el objeto 'usuario'.</comment>
        <title language="es">Titulo de visualizacion para el objeto 'usuario'.</title>
        <!-- Campos del objeto para visualizacion -->
        <fields>
            <!-- - Campos visibles -->
            <!-- El orden es el de aparicion de los tags. Para editar el orden se recomienda utilizar el XMLSpy, -->
            <!-- mediante el cual, es sencillo mover un tag arriba y abajo. -->
            <!-- Todos los atributos son modificables, pero algunos de ellos (label por ejemplo), se deberian modificar en otro sitio -->
            <!-- Los atributos modificables son: 'default' y 'visible'.  -->
            <visible-fields>
                <field name="username" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">Usercode</caption>
                </field>
            </visible-fields>
            <!-- - Campos NO visibles - NO MODIFICAR, ES SOLO A NIVEL INFORMATIVO -->
            <!-- 1/2 - Campos 'hidden'. -->
            <hidden-fields />
            <!-- 2/2 - Campos 'discarded'. -->
            <discarded-fields />
        </fields>
    </site>
    <site name="ct">
        <label language="es">Etiqueta de visualizacion para el objeto 'usuario'.</label>
        <comment language="es">Descripcion de visualizacion para el objeto 'usuario'.</comment>
        <title language="es">Titulo de visualizacion para el objeto 'usuario'.</title>
        <!-- Campos del objeto para visualizacion -->
        <fields>
            <!-- - Campos visibles -->
            <!-- El orden es el de aparicion de los tags. Para editar el orden se recomienda utilizar el XMLSpy, -->
            <!-- mediante el cual, es sencillo mover un tag arriba y abajo. -->
            <!-- Todos los atributos son modificables, pero algunos de ellos (label por ejemplo), se deberian modificar en otro sitio -->
            <!-- Los atributos modificables son: 'default' y 'visible'.  -->
            <visible-fields>
                <field name="username" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Usercode</caption>
                </field>
            </visible-fields>
            <!-- - Campos NO visibles - NO MODIFICAR, ES SOLO A NIVEL INFORMATIVO -->
            <!-- 1/2 - Campos 'hidden'. -->
            <hidden-fields />
            <!-- 2/2 - Campos 'discarded'. -->
            <discarded-fields />
        </fields>
    </site>
    <site name="es">
        <label language="es">Etiqueta de visualizacion para el objeto 'usuario'.</label>
        <comment language="es">Descripcion de visualizacion para el objeto 'usuario'.</comment>
        <title language="es">Titulo de visualizacion para el objeto 'usuario'.</title>
        <!-- Campos del objeto para visualizacion -->
        <fields>
            <!-- - Campos visibles -->
            <!-- El orden es el de aparicion de los tags. Para editar el orden se recomienda utilizar el XMLSpy, -->
            <!-- mediante el cual, es sencillo mover un tag arriba y abajo. -->
            <!-- Todos los atributos son modificables, pero algunos de ellos (label por ejemplo), se deberian modificar en otro sitio -->
            <!-- Los atributos modificables son: 'default' y 'visible'.  -->
            <visible-fields>
                <field name="username" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Usercode</caption>
                </field>
            </visible-fields>
            <!-- - Campos NO visibles - NO MODIFICAR, ES SOLO A NIVEL INFORMATIVO -->
            <!-- 1/2 - Campos 'hidden'. -->
            <hidden-fields />
            <!-- 2/2 - Campos 'discarded'. -->
            <discarded-fields />
        </fields>
    </site>
</item-list>

