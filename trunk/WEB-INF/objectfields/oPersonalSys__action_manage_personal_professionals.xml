<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- Nota: El nombre del objeto (nombre_del_objeto) es el nombre en minusculas de la tabla en base de datos. 	-->
<!-- Nota: En general, si un atributo de tipo 'boolean' no se encuentra se considera su valor como 'false'. 	-->
<item-list obj="professional" site-number="3">
    <desc>Descripcion para la lista del objeto 'professional'.</desc>
    <!-- Categorizaciones para poder agrupar los ficheros de configuracion. -->
    <!-- Para agregar una categoria poner un atributo 'attName=attValue'.   -->
    <category />
    <site name="en">
        <label language="es">Etiqueta de visualizacion para el objeto 'professional'.</label>
        <comment language="es">Descripcion de visualizacion para el objeto 'professional'.</comment>
        <title language="es">Titulo de visualizacion para el objeto 'professional'.</title>
        <!-- Campos del objeto para visualizacion -->
        <fields>
            <!-- - Campos visibles -->
            <!-- El orden es el de aparicion de los tags. Para editar el orden se recomienda utilizar el XMLSpy, -->
            <!-- mediante el cual, es sencillo mover un tag arriba y abajo. -->
            <!-- Todos los atributos son modificables, pero algunos de ellos (label por ejemplo), se deberian modificar en otro sitio -->
            <!-- Los atributos modificables son: 'default' y 'visible'.  -->
            <visible-fields>
                <field name="current" visible="true" default="true" type-of-field="boolean" size="7">
                    <caption language="en">Current</caption>
                </field>
                <field name="start_date" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">Start date</caption>
                </field>
                <field name="end_date" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">End date</caption>
                </field>
                <field name="type_of_contract" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">Type of contract</caption>
                </field>
                <field name="payroll_institution" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">Payrol Inst.</caption>
                </field>
                <field name="professional_unit" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">Unit</caption>
                </field>
                <field name="research_group" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">Research group</caption>
                </field>
                <field name="position" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">Position</caption>
                </field>
            </visible-fields>
            <!-- - Campos NO visibles - NO MODIFICAR, ES SOLO A NIVEL INFORMATIVO -->
            <!-- 1/2 - Campos 'hidden'. -->
            <hidden-fields />
            <!-- 2/2 - Campos 'discarded'. -->
            <discarded-fields />
        </fields>
    </site>
    <site name="ct">
        <label language="es">Etiqueta de visualizacion para el objeto 'professional'.</label>
        <comment language="es">Descripcion de visualizacion para el objeto 'professional'.</comment>
        <title language="es">Titulo de visualizacion para el objeto 'professional'.</title>
        <!-- Campos del objeto para visualizacion -->
        <fields>
            <!-- - Campos visibles -->
            <!-- El orden es el de aparicion de los tags. Para editar el orden se recomienda utilizar el XMLSpy, -->
            <!-- mediante el cual, es sencillo mover un tag arriba y abajo. -->
            <!-- Todos los atributos son modificables, pero algunos de ellos (label por ejemplo), se deberian modificar en otro sitio -->
            <!-- Los atributos modificables son: 'default' y 'visible'.  -->
            <visible-fields>
                <field name="current" visible="true" default="true" type-of-field="boolean" size="7">
                    <caption language="ct">Actual</caption>
                </field>
                <field name="start_date" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Data d'inici</caption>
                </field>
                <field name="end_date" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Data de fi</caption>
                </field>
                <field name="type_of_contract" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Tipus de relació</caption>
                </field>
                <field name="payroll_institution" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Inst. pagadora</caption>
                </field>
                <field name="professional_unit" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Unitat</caption>
                </field>
                <field name="research_group" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Grup de recerca</caption>
                </field>
                <field name="position" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Càrrec</caption>
                </field>
            </visible-fields>
            <!-- - Campos NO visibles - NO MODIFICAR, ES SOLO A NIVEL INFORMATIVO -->
            <!-- 1/2 - Campos 'hidden'. -->
            <hidden-fields />
            <!-- 2/2 - Campos 'discarded'. -->
            <discarded-fields />
        </fields>
    </site>
    <site name="es">
        <label language="es">Etiqueta de visualizacion para el objeto 'professional'.</label>
        <comment language="es">Descripcion de visualizacion para el objeto 'professional'.</comment>
        <title language="es">Titulo de visualizacion para el objeto 'professional'.</title>
        <!-- Campos del objeto para visualizacion -->
        <fields>
            <!-- - Campos visibles -->
            <!-- El orden es el de aparicion de los tags. Para editar el orden se recomienda utilizar el XMLSpy, -->
            <!-- mediante el cual, es sencillo mover un tag arriba y abajo. -->
            <!-- Todos los atributos son modificables, pero algunos de ellos (label por ejemplo), se deberian modificar en otro sitio -->
            <!-- Los atributos modificables son: 'default' y 'visible'.  -->
            <visible-fields>
                <field name="current" visible="true" default="true" type-of-field="boolean" size="7">
                    <caption language="es">Actual</caption>
                </field>
                <field name="start_date" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Fecha de inicio</caption>
                </field>
                <field name="end_date" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Fecha de fin</caption>
                </field>
                <field name="type_of_contract" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Tipo de relación</caption>
                </field>
                <field name="payroll_institution" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Inst. pagadora</caption>
                </field>
                <field name="professional_unit" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Unidad</caption>
                </field>
                <field name="research_group" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Grupo de investigación</caption>
                </field>
                <field name="position" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Cargo</caption>
                </field>
            </visible-fields>
            <!-- - Campos NO visibles - NO MODIFICAR, ES SOLO A NIVEL INFORMATIVO -->
            <!-- 1/2 - Campos 'hidden'. -->
            <hidden-fields />
            <!-- 2/2 - Campos 'discarded'. -->
            <discarded-fields />
        </fields>
    </site>
</item-list>

