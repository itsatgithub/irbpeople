<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- Nota: El nombre del objeto (nombre_del_objeto) es el nombre en minusculas de la tabla en base de datos. 	-->
<!-- Nota: En general, si un atributo de tipo 'boolean' no se encuentra se considera su valor como 'false'. 	-->
<item-list obj="user_access" site-number="3">
    <desc>Descripcion para la lista del objeto 'user_access'.</desc>
    <!-- Categorizaciones para poder agrupar los ficheros de configuracion. -->
    <!-- Para agregar una categoria poner un atributo 'attName=attValue'.   -->
    <category />
    <site name="en">
        <label language="es">Etiqueta de visualizacion para el objeto 'user_access'.</label>
        <comment language="es">Descripcion de visualizacion para el objeto 'user_access'.</comment>
        <title language="es">Titulo de visualizacion para el objeto 'user_access'.</title>
        <!-- Campos del objeto para visualizacion -->
        <fields>
            <!-- - Campos visibles -->
            <!-- El orden es el de aparicion de los tags. Para editar el orden se recomienda utilizar el XMLSpy, -->
            <!-- mediante el cual, es sencillo mover un tag arriba y abajo. -->
            <!-- Todos los atributos son modificables, pero algunos de ellos (label por ejemplo), se deberian modificar en otro sitio -->
            <!-- Los atributos modificables son: 'default' y 'visible'.  -->
            <visible-fields>
                <field name="usercode" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">User code</caption>
                </field>
                <field name="name" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="en">Name</caption>
                </field>
                <field name="locked" visible="true" default="true" type-of-field="boolean" size="10">
                    <caption language="en">Locked</caption>
                </field>
                <field name="tries" visible="true" default="true" type-of-field="integer" size="10">
                    <caption language="en">Tries</caption>
                </field>
                <field name="last_access" visible="true" default="true" type-of-field="date" size="10">
                    <caption language="en">Last access</caption>
                </field>                                
            </visible-fields>
            <!-- - Campos NO visibles - NO MODIFICAR, ES SOLO A NIVEL INFORMATIVO -->
            <!-- 1/2 - Campos 'hidden'. -->
            <hidden-fields/>                
            <!-- 2/2 - Campos 'discarded'. -->
            <discarded-fields />
        </fields>
    </site>
    <site name="ct">
        <label language="es">Etiqueta de visualizacion para el objeto 'user_access'.</label>
        <comment language="es">Descripcion de visualizacion para el objeto 'user_access'.</comment>
        <title language="es">Titulo de visualizacion para el objeto 'user_access'.</title>
        <!-- Campos del objeto para visualizacion -->
        <fields>
            <!-- - Campos visibles -->
            <!-- El orden es el de aparicion de los tags. Para editar el orden se recomienda utilizar el XMLSpy, -->
            <!-- mediante el cual, es sencillo mover un tag arriba y abajo. -->
            <!-- Todos los atributos son modificables, pero algunos de ellos (label por ejemplo), se deberian modificar en otro sitio -->
            <!-- Los atributos modificables son: 'default' y 'visible'.  -->
            <visible-fields>
                <field name="usercode" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">User code</caption>
                </field>
                <field name="name" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="ct">Name</caption>
                </field>
                <field name="locked" visible="true" default="true" type-of-field="boolean" size="10">
                    <caption language="ct">Locked</caption>
                </field>
                <field name="tries" visible="true" default="true" type-of-field="integer" size="10">
                    <caption language="ct">Tries</caption>
                </field>
                <field name="last_access" visible="true" default="true" type-of-field="date" size="10">
                    <caption language="ct">Last access</caption>
                </field>
            </visible-fields>
            <!-- - Campos NO visibles - NO MODIFICAR, ES SOLO A NIVEL INFORMATIVO -->
            <!-- 1/2 - Campos 'hidden'. -->
            <hidden-fields />
            <!-- 2/2 - Campos 'discarded'. -->
            <discarded-fields />
        </fields>
    </site>
    <site name="es">
        <label language="es">Etiqueta de visualizacion para el objeto 'user_access'.</label>
        <comment language="es">Descripcion de visualizacion para el objeto 'user_access'.</comment>
        <title language="es">Titulo de visualizacion para el objeto 'user_access'.</title>
        <!-- Campos del objeto para visualizacion -->
        <fields>
            <!-- - Campos visibles -->
            <!-- El orden es el de aparicion de los tags. Para editar el orden se recomienda utilizar el XMLSpy, -->
            <!-- mediante el cual, es sencillo mover un tag arriba y abajo. -->
            <!-- Todos los atributos son modificables, pero algunos de ellos (label por ejemplo), se deberian modificar en otro sitio -->
            <!-- Los atributos modificables son: 'default' y 'visible'.  -->
            <visible-fields>
               <field name="usercode" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">User code</caption>
                </field>
                <field name="name" visible="true" default="true" type-of-field="text" size="20">
                    <caption language="es">Name</caption>
                </field>
                <field name="locked" visible="true" default="true" type-of-field="boolean" size="10">
                    <caption language="es">Locked</caption>
                </field>
                <field name="tries" visible="true" default="true" type-of-field="integer" size="10">
                    <caption language="es">Tries</caption>
                </field>
                <field name="last_access" visible="true" default="true" type-of-field="date" size="10">
                    <caption language="es">Last access</caption>
                </field>
            </visible-fields>
            <!-- - Campos NO visibles - NO MODIFICAR, ES SOLO A NIVEL INFORMATIVO -->
            <!-- 1/2 - Campos 'hidden'. -->
            <hidden-fields/>
            <!-- 2/2 - Campos 'discarded'. -->
            <discarded-fields />
        </fields>
    </site>
</item-list>

